# CS2 Server RCON Configuration
RCON_HOST=192.168.1.50
RCON_PORT=27015
RCON_PASS=your_rcon_password_here

# Note: the player-count poll interval (while waiting for an empty server) is
# hardcoded to 60 seconds and is not configurable via environment variables.
