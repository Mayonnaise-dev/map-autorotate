services:
  autorotate:
    build: .
    container_name: autorotate
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - cs2-network
networks:
  cs2-network:
    external: true
